# プロジェクト構成設計

## 📁 ディレクトリ構造

```
gas-skillup-app/
├── app/                          # Next.js App Router
│   ├── (auth)/                   # 認証関連（グループルート）
│   │   ├── login/
│   │   │   └── page.tsx          # ログイン画面
│   │   └── pending-approval/
│   │       └── page.tsx          # 承認待ち画面
│   │
│   ├── (student)/                # 受講生向け（グループルート）
│   │   ├── layout.tsx            # 受講生レイアウト
│   │   ├── page.tsx              # ホーム画面
│   │   ├── contents/
│   │   │   ├── page.tsx          # コンテンツ一覧
│   │   │   └── [id]/
│   │   │       └── page.tsx      # コンテンツ詳細
│   │   ├── submissions/
│   │   │   ├── page.tsx          # 課題履歴
│   │   │   └── new/
│   │   │       └── page.tsx      # 課題提出フォーム
│   │   └── announcements/
│   │       └── page.tsx          # お知らせ一覧
│   │
│   ├── (instructor)/             # 運営向け（グループルート）
│   │   ├── layout.tsx            # 運営レイアウト
│   │   ├── dashboard/
│   │   │   └── page.tsx          # ダッシュボード
│   │   ├── students/
│   │   │   ├── page.tsx          # 受講生一覧
│   │   │   └── [id]/
│   │   │       └── page.tsx      # 受講生詳細
│   │   ├── approvals/
│   │   │   └── page.tsx          # 承認管理
│   │   ├── submissions/
│   │   │   ├── page.tsx          # 課題管理
│   │   │   └── [id]/
│   │   │       └── page.tsx      # 課題詳細・フィードバック
│   │   ├── contents/
│   │   │   ├── page.tsx          # コンテンツ管理
│   │   │   └── [id]/
│   │   │       └── edit/
│   │   │           └── page.tsx  # コンテンツ編集
│   │   └── announcements/
│   │       └── page.tsx          # お知らせ管理
│   │
│   ├── api/                      # API Routes
│   │   ├── auth/
│   │   │   └── callback/
│   │   │       └── route.ts      # OAuth callback
│   │   ├── users/
│   │   │   ├── route.ts          # ユーザー一覧・作成
│   │   │   └── [id]/
│   │   │       ├── route.ts      # ユーザー取得・更新・削除
│   │   │       └── approve/
│   │   │           └── route.ts  # ユーザー承認
│   │   ├── contents/
│   │   │   ├── route.ts
│   │   │   └── [id]/
│   │   │       ├── route.ts
│   │   │       └── progress/
│   │   │           └── route.ts  # 進捗記録
│   │   ├── submissions/
│   │   │   ├── route.ts
│   │   │   └── [id]/
│   │   │       └── feedback/
│   │   │           └── route.ts  # フィードバック
│   │   └── announcements/
│   │       └── route.ts
│   │
│   ├── layout.tsx                # ルートレイアウト
│   ├── globals.css               # グローバルスタイル
│   ├── error.tsx                 # エラーページ
│   └── not-found.tsx             # 404ページ
│
├── components/                   # コンポーネント
│   ├── ui/                       # 共通UIコンポーネント
│   │   ├── button.tsx
│   │   ├── input.tsx
│   │   ├── card.tsx
│   │   ├── badge.tsx
│   │   ├── progress-bar.tsx
│   │   ├── spinner.tsx
│   │   └── empty-state.tsx
│   │
│   ├── layout/                   # レイアウトコンポーネント
│   │   ├── header.tsx
│   │   ├── footer.tsx
│   │   ├── sidebar.tsx
│   │   └── navigation.tsx
│   │
│   ├── student/                  # 受講生向けコンポーネント
│   │   ├── content-card.tsx
│   │   ├── progress-summary.tsx
│   │   ├── submission-form.tsx
│   │   └── announcement-card.tsx
│   │
│   └── instructor/               # 運営向けコンポーネント
│       ├── student-table.tsx
│       ├── submission-list.tsx
│       ├── stats-card.tsx
│       └── content-editor.tsx
│
├── lib/                          # ユーティリティ・ロジック
│   ├── supabase/                 # Supabase関連
│   │   ├── client.ts             # クライアント初期化
│   │   ├── server.ts             # サーバーサイドクライアント
│   │   └── middleware.ts         # 認証ミドルウェア
│   │
│   ├── hooks/                    # カスタムフック
│   │   ├── use-user.ts           # ユーザー情報フック
│   │   ├── use-contents.ts       # コンテンツ取得フック
│   │   └── use-progress.ts       # 進捗管理フック
│   │
│   ├── mock/                     # モックデータ（Sprint 1-2用）
│   │   ├── users.ts
│   │   ├── contents.ts
│   │   ├── submissions.ts
│   │   └── announcements.ts
│   │
│   └── utils/                    # ユーティリティ関数
│       ├── format.ts             # 日付・文字列フォーマット
│       ├── validation.ts         # バリデーション
│       └── cn.ts                 # classNames ユーティリティ
│
├── types/                        # TypeScript型定義
│   ├── database.types.ts         # Supabase生成型
│   ├── models.ts                 # ビジネスロジック型
│   └── api.ts                    # API型定義
│
├── middleware.ts                 # Next.js ミドルウェア（認証チェック）
│
├── docs/                         # ドキュメント
│   ├── requirement.md
│   ├── workflow.md
│   └── design/
│       ├── screen-transition.md
│       ├── database-design.md
│       └── project-structure.md  # このファイル
│
└── public/                       # 静的ファイル
    └── images/
```

## 🎯 設計方針

### 1. App Router グループルート

Next.js App Router の**ルートグループ**機能を活用：

- `(auth)`: 認証関連画面（レイアウトなし）
- `(student)`: 受講生向け画面（専用レイアウト）
- `(instructor)`: 運営向け画面（専用レイアウト）

**メリット**:
- URLには影響しない（例: `/login` は `(auth)/login` から生成）
- グループごとに異なるレイアウトを適用可能
- 論理的な画面分離

### 2. コンポーネント分割

```
components/
├── ui/          → 再利用可能な汎用UIコンポーネント
├── layout/      → ヘッダー、フッター、ナビゲーション
├── student/     → 受講生向けドメインコンポーネント
└── instructor/  → 運営向けドメインコンポーネント
```

**原則**:
- `ui/`: デザインシステムの基盤（shadcn/ui風）
- `layout/`: 全体構造を担当
- `student/`, `instructor/`: ビジネスロジック含む特化コンポーネント

### 3. 状態管理・データフェッチング

**フェーズ1（Sprint 1-2）: モックデータ**
```typescript
// lib/mock/users.ts でモックデータ生成
import { mockUsers } from '@/lib/mock/users'
```

**フェーズ2（Sprint 3以降）: Supabase**
```typescript
// lib/hooks/use-user.ts でデータフェッチ
import { useUser } from '@/lib/hooks/use-user'
```

### 4. 型定義戦略

```typescript
// types/database.types.ts - Supabaseから自動生成
export type Database = { ... }

// types/models.ts - ビジネスロジック用
export type User = Database['public']['Tables']['users']['Row']
export type Phase = Database['public']['Tables']['phases']['Row']

// types/api.ts - API リクエスト/レスポンス
export type CreateUserRequest = { ... }
export type CreateUserResponse = { ... }
```

## 📦 依存パッケージ（追加予定）

### Sprint 0（基盤）
```json
{
  "dependencies": {
    "clsx": "^2.0.0",
    "tailwind-merge": "^2.0.0"
  }
}
```

### Sprint 3（バックエンド統合時）
```json
{
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "@supabase/ssr": "^0.1.0"
  }
}
```

### オプション（検討中）
```json
{
  "dependencies": {
    "@tanstack/react-query": "^5.0.0",  // データフェッチング
    "react-hook-form": "^7.49.0",        // フォーム管理
    "zod": "^3.22.0",                    // バリデーション
    "recharts": "^2.10.0"                // チャート（ダッシュボード用）
  }
}
```

---

**改訂履歴**

| 日付 | 内容 |
|------|------|
| 2024年12月 | 初版作成 |
