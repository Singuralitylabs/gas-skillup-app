# プロジェクト構成設計

## 📁 ディレクトリ構造

```
gas-skillup-app/
├── app/                          # Next.js App Router（アプリケーション全体）
│   ├── _components/              # アプリ全体の共通コンポーネント
│   │   ├── ui/                   # 共通UIコンポーネント
│   │   │   ├── button.tsx
│   │   │   ├── input.tsx
│   │   │   ├── textarea.tsx
│   │   │   ├── select.tsx
│   │   │   ├── card.tsx
│   │   │   ├── badge.tsx
│   │   │   ├── progress.tsx
│   │   │   ├── loading-spinner.tsx
│   │   │   ├── empty-state.tsx
│   │   │   ├── breadcrumbs.tsx
│   │   │   └── index.ts          # エクスポート集約
│   │   │
│   │   └── layout/               # レイアウトコンポーネント
│   │       ├── header.tsx
│   │       ├── footer.tsx
│   │       ├── sidebar.tsx
│   │       ├── mobile-menu.tsx
│   │       ├── navigation.tsx
│   │       └── index.ts          # エクスポート集約
│   │
│   ├── _lib/                     # ロジック・ユーティリティ
│   │   ├── supabase/             # Supabase関連
│   │   │   ├── client.ts         # クライアント初期化
│   │   │   ├── server.ts         # サーバーサイドクライアント
│   │   │   └── middleware.ts     # 認証ミドルウェア
│   │   │
│   │   ├── hooks/                # カスタムフック
│   │   │   ├── use-user.ts       # ユーザー情報フック
│   │   │   ├── use-contents.ts   # コンテンツ取得フック
│   │   │   └── use-progress.ts   # 進捗管理フック
│   │   │
│   │   ├── mock/                 # モックデータ（Sprint 1-2用）
│   │   │   ├── users.ts
│   │   │   ├── contents.ts
│   │   │   ├── submissions.ts
│   │   │   ├── announcements.ts
│   │   │   └── index.ts          # エクスポート集約
│   │   │
│   │   └── utils/                # ユーティリティ関数
│   │       ├── format.ts         # 日付・文字列フォーマット
│   │       ├── validation.ts     # バリデーション
│   │       ├── cn.ts             # classNames ユーティリティ
│   │       └── index.ts          # エクスポート集約
│   │
│   ├── _types/                   # TypeScript型定義
│   │   ├── database.types.ts     # Supabase生成型
│   │   ├── models.ts             # ビジネスロジック型
│   │   ├── api.ts                # API型定義
│   │   └── index.ts              # エクスポート集約
│   │
│   ├── (auth)/                   # 認証関連（グループルート）
│   │   ├── login/
│   │   │   └── page.tsx          # ログイン画面
│   │   └── pending-approval/
│   │       └── page.tsx          # 承認待ち画面
│   │
│   ├── (student)/                # 受講生向け（グループルート）
│   │   ├── _components/          # 受講生向け専用コンポーネント
│   │   │   ├── content-card.tsx
│   │   │   ├── phase-section.tsx
│   │   │   ├── week-section.tsx
│   │   │   ├── progress-card.tsx
│   │   │   ├── submission-form.tsx
│   │   │   ├── submission-card.tsx
│   │   │   ├── announcement-card.tsx
│   │   │   ├── youtube-player.tsx
│   │   │   ├── markdown-viewer.tsx
│   │   │   └── index.ts          # エクスポート集約
│   │   │
│   │   ├── layout.tsx            # 受講生レイアウト
│   │   ├── page.tsx              # ホーム画面
│   │   ├── contents/
│   │   │   ├── page.tsx          # コンテンツ一覧
│   │   │   └── [id]/
│   │   │       └── page.tsx      # コンテンツ詳細
│   │   ├── submissions/
│   │   │   ├── page.tsx          # 課題履歴
│   │   │   └── new/
│   │   │       └── page.tsx      # 課題提出フォーム
│   │   └── announcements/
│   │       └── page.tsx          # お知らせ一覧
│   │
│   ├── (instructor)/             # 運営向け（グループルート）
│   │   ├── _components/          # 運営向け専用コンポーネント
│   │   │   ├── student-table.tsx
│   │   │   ├── submission-list.tsx
│   │   │   ├── stats-card.tsx
│   │   │   ├── content-editor.tsx
│   │   │   └── index.ts          # エクスポート集約
│   │   │
│   │   ├── layout.tsx            # 運営レイアウト
│   │   ├── dashboard/
│   │   │   └── page.tsx          # ダッシュボード
│   │   ├── students/
│   │   │   ├── page.tsx          # 受講生一覧
│   │   │   └── [id]/
│   │   │       └── page.tsx      # 受講生詳細
│   │   ├── approvals/
│   │   │   └── page.tsx          # 承認管理
│   │   ├── submissions/
│   │   │   ├── page.tsx          # 課題管理
│   │   │   └── [id]/
│   │   │       └── page.tsx      # 課題詳細・フィードバック
│   │   ├── contents/
│   │   │   ├── page.tsx          # コンテンツ管理
│   │   │   └── [id]/
│   │   │       └── edit/
│   │   │           └── page.tsx  # コンテンツ編集
│   │   └── announcements/
│   │       └── page.tsx          # お知らせ管理
│   │
│   ├── api/                      # API Routes
│   │   ├── auth/
│   │   │   └── callback/
│   │   │       └── route.ts      # OAuth callback
│   │   ├── users/
│   │   │   ├── route.ts          # ユーザー一覧・作成
│   │   │   └── [id]/
│   │   │       ├── route.ts      # ユーザー取得・更新・削除
│   │   │       └── approve/
│   │   │           └── route.ts  # ユーザー承認
│   │   ├── contents/
│   │   │   ├── route.ts
│   │   │   └── [id]/
│   │   │       ├── route.ts
│   │   │       └── progress/
│   │   │           └── route.ts  # 進捗記録
│   │   ├── submissions/
│   │   │   ├── route.ts
│   │   │   └── [id]/
│   │   │       └── feedback/
│   │   │           └── route.ts  # フィードバック
│   │   └── announcements/
│   │       └── route.ts
│   │
│   ├── layout.tsx                # ルートレイアウト
│   ├── page.tsx                  # ルートページ
│   ├── globals.css               # グローバルスタイル
│   ├── error.tsx                 # エラーページ
│   └── not-found.tsx             # 404ページ
│
├── middleware.ts                 # Next.js ミドルウェア（認証チェック）
│
├── docs/                         # プロジェクトドキュメント
│   ├── requirement.md
│   ├── workflow.md
│   └── design/
│       ├── screen-transition.md
│       ├── database-design.md
│       └── project-structure.md  # このファイル
│
├── public/                       # 静的ファイル
│   └── images/
│
├── package.json                  # パッケージ設定
├── tsconfig.json                 # TypeScript設定
├── biome.json                    # Biome設定
├── tailwind.config.ts            # Tailwind CSS設定
├── postcss.config.mjs            # PostCSS設定
└── next.config.ts                # Next.js設定
```

## 🎯 設計方針

### 1. 完全コロケーション戦略

すべてのアプリケーションコードを `app/` ディレクトリ配下に集約：

```
app/
├── _components/    # UIコンポーネント
├── _lib/           # ロジック・ユーティリティ
├── _types/         # 型定義
├── (auth)/         # 認証ページ
├── (student)/      # 受講生向けページ
├── (instructor)/   # 運営向けページ
└── api/            # API Routes
```

**メリット**:
- ✅ **一貫性**: すべてのアプリコードが `app/` 配下
- ✅ **コロケーション**: 関連するコードが近い場所に配置
- ✅ **Next.js App Router 準拠**: 公式の推奨パターン
- ✅ **明確な境界**: `app/` = アプリケーション、それ以外 = 設定ファイル
- ✅ **スケーラブル**: 機能追加時にルールが明確

### 2. App Router グループルート

Next.js App Router の**ルートグループ**機能を活用：

- `(auth)`: 認証関連画面（レイアウトなし）
- `(student)`: 受講生向け画面（専用レイアウト）
- `(instructor)`: 運営向け画面（専用レイアウト）

**メリット**:
- ✅ URLには影響しない（例: `/login` は `(auth)/login` から生成）
- ✅ グループごとに異なるレイアウトを適用可能
- ✅ 論理的な画面分離

### 3. プライベートフォルダ（`_` プレフィックス）

ルーティングに影響しないフォルダに `_` を使用：

- `app/_components/`: ルーティング対象外の共通コンポーネント
- `app/_lib/`: ルーティング対象外のユーティリティ
- `app/_types/`: ルーティング対象外の型定義
- `app/(student)/_components/`: 受講生向け専用コンポーネント

**メリット**:
- ✅ 明示的にルーティング除外を宣言
- ✅ 誤って `page.tsx` を配置しても安全
- ✅ コードの意図が明確

### 4. コンポーネント階層

```
app/
├── _components/              # 🌐 アプリ全体の共通コンポーネント
│   ├── ui/                   # 基本UIコンポーネント（shadcn/ui風）
│   └── layout/               # ヘッダー、フッター、ナビゲーション
│
├── (student)/_components/    # 🎓 受講生向け専用コンポーネント
│   ├── content-card.tsx      # ドメインロジックを含む
│   └── submission-form.tsx   # ビジネスルールを含む
│
└── (instructor)/_components/ # 👨‍🏫 運営向け専用コンポーネント
    ├── student-table.tsx     # 管理画面専用UI
    └── stats-card.tsx        # 統計表示
```

**原則**:
- `_components/ui/`: 再利用可能な汎用UI（プロジェクト横断）
- `_components/layout/`: アプリ全体の構造（ヘッダー、フッター等）
- `(group)/_components/`: グループ専用のドメインコンポーネント

### 5. 状態管理・データフェッチング

**フェーズ1（Sprint 1-2）: モックデータ**
```typescript
// app/_lib/mock/users.ts でモックデータ生成
import { mockUsers } from '@/lib/mock/users'
```

**フェーズ2（Sprint 3以降）: Supabase**
```typescript
// app/_lib/hooks/use-user.ts でデータフェッチ
import { useUser } from '@/lib/hooks/use-user'
```

### 6. 型定義戦略

```typescript
// app/_types/database.types.ts - Supabaseから自動生成
export type Database = { ... }

// app/_types/models.ts - ビジネスロジック用
export type User = Database['public']['Tables']['users']['Row']
export type Phase = Database['public']['Tables']['phases']['Row']

// app/_types/api.ts - API リクエスト/レスポンス
export type CreateUserRequest = { ... }
export type CreateUserResponse = { ... }
```

### 7. エクスポート集約パターン

各ディレクトリに `index.ts` を配置してエクスポートを集約：

```typescript
// app/_components/ui/index.ts
export { Button } from './button'
export { Card, CardHeader, CardContent } from './card'
export { Input } from './input'
export { Badge } from './badge'

// 使用側
import { Button, Card, Input } from '@/components/ui'
```

**メリット**:
- ✅ インポート文がシンプルに
- ✅ 依存関係が明確
- ✅ リファクタリングが容易

## 📦 依存パッケージ（追加予定）

### Sprint 0（基盤）
```json
{
  "dependencies": {
    "clsx": "^2.0.0",
    "tailwind-merge": "^2.0.0"
  }
}
```

### Sprint 3（バックエンド統合時）
```json
{
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "@supabase/ssr": "^0.1.0"
  }
}
```

### オプション（検討中）
```json
{
  "dependencies": {
    "@tanstack/react-query": "^5.0.0",  // データフェッチング
    "react-hook-form": "^7.49.0",        // フォーム管理
    "zod": "^3.22.0",                    // バリデーション
    "recharts": "^2.10.0"                // チャート（ダッシュボード用）
  }
}
```

**改訂履歴**

| 日付 | バージョン | 内容 |
|------|----------|------|
| 2024年12月 | v1.0 | 初版作成 |
| 2024年12月 | v2.0 | 完全コロケーション戦略に基づき全面改訂 |
